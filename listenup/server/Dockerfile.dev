FROM node:18-alpine

# 개발 도구 설치
RUN apk add --no-cache git

WORKDIR /app

# 패키지 파일 먼저 복사 (Docker 레이어 캐싱 최적화)
COPY package*.json ./

# 의존성 설치
RUN npm install

# TypeScript 전역 설치 (개발 편의성)
RUN npm install -g typescript tsx

# 소스 코드 복사 (볼륨 마운트로 실시간 업데이트)
COPY . .

# 포트 노출
EXPOSE 3000

# 개발 서버 실행 (tsx watch로 핫 리로드)
CMD ["npm", "run", "dev"]