# 클라이언트 리팩터링 보고서

**작성일**: 2025-11-13
**프로젝트**: ListenUp! - 실시간 음악 맞추기 게임
**작업 범위**: 클라이언트 코드베이스 구조 개선 및 컴포넌트 분리

---

## 목차

1. [작업 개요](#작업-개요)
2. [리팩터링 목표](#리팩터링-목표)
3. [아키텍처 설계](#아키텍처-설계)
4. [Game 페이지 리팩터링](#game-페이지-리팩터링)
5. [Playlist 페이지 리팩터링](#playlist-페이지-리팩터링)
6. [App.svelte 간소화](#appsvelte-간소화)
7. [개선 효과](#개선-효과)
8. [코드 메트릭](#코드-메트릭)
9. [향후 개선 사항](#향후-개선-사항)

---

## 작업 개요

### 프로젝트 정보
- **작업 일자**: 2025-11-13
- **작업자**: ListenUp! Team
- **목적**: 대규모 단일 파일을 기능별로 분리하여 유지보수성 향상

### 구현 배경

기존 클라이언트 코드는 다음과 같은 문제점이 있었습니다:

1. **거대한 단일 파일**:
   - `App.svelte`: 1,220 라인 (게임 + 플레이리스트 관리 모두 포함)
   - `PlaylistManager.svelte`: 1,229 라인 (모든 기능이 하나의 파일에)

2. **낮은 가독성**:
   - 기능 파악이 어려움
   - 코드 수정 시 연관 로직 찾기 힘듦
   - 새로운 팀원의 온보딩 어려움

3. **높은 결합도**:
   - 게임 로직과 UI가 혼재
   - 재사용 가능한 컴포넌트 식별 어려움

4. **테스트 어려움**:
   - 단위 테스트 불가능
   - 통합 테스트만 가능

이러한 문제를 해결하기 위해 **기능별 폴더 구조**와 **컴포넌트 분리** 전략을 도입했습니다.

---

## 리팩터링 목표

### 핵심 요구사항

1. ✅ 페이지별 폴더 구조 (`game/`, `playlist/`)
2. ✅ 각 파일 크기 1,000 라인 이하로 제한
3. ✅ 재사용 가능한 컴포넌트 분리
4. ✅ Props 패턴으로 명확한 데이터 흐름
5. ✅ 모든 기능 유지 (기능 손실 없음)
6. ✅ 빌드 성공 (타입 오류 없음)

### 기술적 목표

- Svelte 5 Runes API 활용 ($state, $derived)
- TypeScript 인터페이스로 Props 타입 정의
- 명확한 단일 책임 원칙 적용
- 컴포넌트 간 느슨한 결합

---

## 아키텍처 설계

### Before: 단일 파일 구조

```
packages/client/src/
├── App.svelte (1,220 lines)
│   ├── 게임 로비 UI
│   ├── 게임 룸 UI
│   ├── YouTube Player 관리
│   ├── 게임 결과 UI
│   ├── 플레이리스트 관리 UI
│   └── Socket.IO 통신
├── lib/
│   └── components/
│       └── PlaylistManager.svelte (1,229 lines)
└── main.ts
```

**문제점**:
- App.svelte에 게임과 라우팅이 모두 포함
- PlaylistManager.svelte가 너무 큼
- 기능별 분리가 안됨

---

### After: 기능별 폴더 구조

```
packages/client/src/
├── App.svelte (86 lines)          ← 93% 감소!
│   └── 페이지 라우팅만 담당
│
├── lib/
│   └── pages/
│       ├── game/
│       │   ├── Game.svelte (715 lines)
│       │   │   └── 게임 로직 및 Socket.IO 통신
│       │   ├── GameLobby.svelte (180 lines)
│       │   │   └── 방 생성/입장 UI
│       │   ├── GameRoom.svelte (350 lines)
│       │   │   └── 게임 진행 UI
│       │   ├── GamePlayer.svelte (108 lines)
│       │   │   └── YouTube 플레이어
│       │   ├── GameResult.svelte (100 lines)
│       │   │   └── 게임 결과 화면
│       │   └── gameStore.ts
│       │       └── 게임 상태 관리
│       │
│       └── playlist/
│           ├── Playlist.svelte (760 lines)
│           │   └── 플레이리스트 관리 로직
│           ├── PlaylistForm.svelte (147 lines)
│           │   └── 생성/수정 모달
│           ├── TrackForm.svelte (327 lines)
│           │   └── 트랙 추가 모달
│           └── TrackList.svelte (332 lines)
│               └── 트랙 목록 및 수정
│
├── app.css
├── main.ts
└── vite-env.d.ts
```

**개선점**:
- ✅ 페이지별로 명확히 분리
- ✅ 각 파일이 적절한 크기
- ✅ 기능별 응집도 향상
- ✅ 재사용 가능한 컴포넌트 식별 용이

---

### 폴더 구조 설계 원칙

#### 1. Pages 기반 구조

```
lib/pages/
├── game/          ← 게임 관련 모든 것
├── playlist/      ← 플레이리스트 관련 모든 것
└── (future pages) ← 확장 가능
```

**장점**:
- 새로운 페이지 추가 시 독립적인 폴더 생성
- 페이지별 컴포넌트 충돌 방지
- 팀원들이 작업 영역 명확히 구분

#### 2. 컴포넌트 분리 기준

```
Playlist.svelte (Parent)
├── PlaylistForm.svelte      ← Modal UI
├── TrackForm.svelte          ← Modal UI
└── TrackList.svelte          ← List UI
```

**분리 기준**:
- 독립적인 UI 섹션 (Modal, List, Form)
- 200~400 라인 정도로 관리 가능한 크기
- 재사용 가능성

#### 3. State Management 패턴

```
Game.svelte
├── gameStore.ts              ← 중앙 상태 관리
├── GameLobby.svelte          ← Props로 상태 전달
├── GameRoom.svelte           ← Props로 상태 전달
└── GamePlayer.svelte         ← Props로 상태 전달
```

**패턴**:
- 부모 컴포넌트가 상태 소유
- 자식 컴포넌트는 Props로 상태 수신
- 이벤트 핸들러도 Props로 전달

---

## Game 페이지 리팩터링

### 1. gameStore.ts 생성

#### 역할
- 게임 상태 중앙 관리
- Derived state 계산
- 상태 업데이트 함수 제공

#### 코드

```typescript
import { writable, derived } from "svelte/store";
import type { Socket } from "socket.io-client";

export interface GameState {
  // 연결 상태
  socket: Socket | null;
  connected: boolean;
  statusMessage: string;

  // 방 정보
  roomCode: string;
  nickname: string;
  currentRoom: any | null;
  players: any[];

  // 플레이리스트
  playlists: any[];
  selectedPlaylistId: string;

  // 게임 진행
  gameStarted: boolean;
  currentRound: number;
  totalRounds: number;
  currentTrack: any | null;
  preparedTrack: any | null;

  // YouTube Player
  player: any | null;
  playerReady: boolean;
  isPlaying: boolean;
  volume: number;
  isMuted: boolean;

  // 정답 제출
  userAnswer: string;
  hasSubmitted: boolean;
  answerResult: any | null;

  // 게임 결과
  gameResult: any | null;
}

const initialState: GameState = {
  socket: null,
  connected: false,
  statusMessage: "연결 대기 중...",
  roomCode: "",
  nickname: "",
  currentRoom: null,
  players: [],
  playlists: [],
  selectedPlaylistId: "test-playlist",
  gameStarted: false,
  currentRound: 0,
  totalRounds: 0,
  currentTrack: null,
  preparedTrack: null,
  player: null,
  playerReady: false,
  isPlaying: false,
  volume: 50,
  isMuted: false,
  userAnswer: "",
  hasSubmitted: false,
  answerResult: null,
  gameResult: null,
};

export const gameStore = writable<GameState>(initialState);

// Derived state: 방장 여부
export const isHost = derived(
  gameStore,
  ($store) => $store.currentRoom?.hostId === $store.socket?.id
);

// 상태 업데이트 헬퍼
export function updateGameStore(updates: Partial<GameState>) {
  gameStore.update((state) => ({ ...state, ...updates }));
}
```

**장점**:
- 상태 로직과 UI 로직 분리
- 여러 컴포넌트에서 동일한 상태 접근 가능
- 타입 안정성 확보

---

### 2. GameLobby.svelte

#### 역할
- 방 생성 UI
- 방 입장 UI
- 플레이리스트 선택 UI

#### Props 인터페이스

```typescript
interface Props {
  connected: boolean;
  nickname: string;
  roomCode: string;
  selectedPlaylistId: string;
  playlists: any[];
  onCreateRoom: () => void;
  onJoinRoom: () => void;
  onNicknameChange: (e: Event) => void;
  onRoomCodeChange: (e: Event) => void;
  onPlaylistChange: (e: Event) => void;
}
```

#### UI 구조

```svelte
<div class="lobby">
  <h2>🎮 게임 로비</h2>

  {#if !connected}
    <div class="connection-status">
      서버 연결 대기 중...
    </div>
  {:else}
    <div class="lobby-form">
      <!-- 닉네임 입력 -->
      <input bind:value={nickname} placeholder="닉네임" />

      <!-- 플레이리스트 선택 -->
      <select bind:value={selectedPlaylistId}>
        {#each playlists as playlist}
          <option value={playlist.id}>
            {playlist.name} ({playlist.tracks.length} 트랙)
          </option>
        {/each}
      </select>

      <!-- 방 생성 -->
      <button onclick={onCreateRoom}>
        🏠 방 만들기
      </button>

      <!-- 방 입장 -->
      <input bind:value={roomCode} placeholder="방 코드" />
      <button onclick={onJoinRoom}>
        🚪 방 입장
      </button>
    </div>
  {/if}
</div>
```

**크기**: 180 라인 (적절한 크기)

---

### 3. GameRoom.svelte

#### 역할
- 방 정보 표시
- 플레이어 목록
- 게임 진행 상태
- 정답 제출 UI
- 게임 시작 버튼 (방장만)

#### Props 인터페이스

```typescript
interface Props {
  currentRoom: any;
  isHost: boolean;
  gameStarted: boolean;
  currentRound: number;
  totalRounds: number;
  userAnswer: string;
  hasSubmitted: boolean;
  answerResult: any | null;
  onStartGame: () => void;
  onSubmitAnswer: () => void;
  onAnswerChange: (e: Event) => void;
  onLeaveRoom: () => void;
}
```

#### UI 구조

```svelte
<div class="game-room">
  <!-- 방 정보 -->
  <div class="room-header">
    <h2>방 코드: {currentRoom.code}</h2>
    <button onclick={onLeaveRoom}>🚪 나가기</button>
  </div>

  <!-- 플레이어 목록 -->
  <div class="players-list">
    <h3>플레이어 ({currentRoom.players.length}/{currentRoom.settings.maxPlayers})</h3>
    {#each currentRoom.players as player}
      <div class="player-item">
        {player.nickname}
        {#if player.isHost}👑{/if}
        {#if gameStarted}
          <span class="score">{player.score || 0}점</span>
        {/if}
      </div>
    {/each}
  </div>

  <!-- 게임 진행 -->
  {#if gameStarted}
    <div class="game-progress">
      <h3>라운드 {currentRound} / {totalRounds}</h3>

      <!-- YouTube 플레이어 (GamePlayer 컴포넌트) -->
      <GamePlayer {...playerProps} />

      <!-- 정답 입력 -->
      {#if !hasSubmitted}
        <div class="answer-input">
          <input
            bind:value={userAnswer}
            placeholder="정답을 입력하세요"
            onkeydown={(e) => e.key === "Enter" && onSubmitAnswer()}
          />
          <button onclick={onSubmitAnswer}>제출</button>
        </div>
      {:else}
        <div class="answer-result">
          {answerResult?.message}
        </div>
      {/if}
    </div>
  {:else if isHost}
    <button class="btn-start-game" onclick={onStartGame}>
      🎮 게임 시작
    </button>
  {/if}

  <!-- 게임 결과 (GameResult 컴포넌트) -->
  {#if gameResult}
    <GameResult {gameResult} />
  {/if}
</div>
```

**크기**: 350 라인 (적절한 크기)

---

### 4. GamePlayer.svelte

#### 역할
- YouTube 플레이어 렌더링
- 볼륨 조절
- 음소거 토글

#### Props 인터페이스

```typescript
interface Props {
  preparedTrack: any | null;
  currentTrack: any | null;
  playerReady: boolean;
  volume: number;
  isMuted: boolean;
  onVolumeChange: (e: Event) => void;
  onToggleMute: () => void;
}
```

#### UI 구조

```svelte
<div class="youtube-player-container">
  {#if preparedTrack || currentTrack}
    <!-- YouTube iframe (숨김) -->
    <div class="youtube-player-hidden">
      <div id="youtube-player"></div>
    </div>

    <!-- 볼륨 컨트롤 -->
    {#if !isMuted}
      <div class="volume-control">
        <label>
          🔊 볼륨: {volume}%
          <input
            type="range"
            min="0"
            max="100"
            value={volume}
            oninput={onVolumeChange}
          />
        </label>
      </div>
    {/if}

    <button onclick={onToggleMute}>
      {isMuted ? "🔇 음소거 해제" : "🔊 음소거"}
    </button>
  {/if}
</div>
```

**크기**: 108 라인 (적절한 크기)

---

### 5. GameResult.svelte

#### 역할
- 게임 종료 화면
- 최종 순위 표시
- 우승자 표시

#### Props 인터페이스

```typescript
interface Props {
  gameResult: {
    winner: {
      nickname: string;
      score: number;
    } | null;
    finalScores: Array<{
      nickname: string;
      score: number;
      correctAnswers: number;
    }>;
  };
}
```

#### UI 구조

```svelte
<div class="game-result-modal">
  <div class="result-content">
    <h2>🎉 게임 종료!</h2>

    {#if gameResult.winner}
      <div class="winner">
        <h3>👑 우승자: {gameResult.winner.nickname}</h3>
        <p class="winner-score">{gameResult.winner.score}점</p>
      </div>
    {/if}

    <div class="final-scores">
      <h3>최종 순위</h3>
      {#each gameResult.finalScores as player, index}
        <div class="score-item">
          <span class="rank">#{index + 1}</span>
          <span class="player-name">{player.nickname}</span>
          <span class="player-score">{player.score}점</span>
          <span class="correct-count">
            ({player.correctAnswers} 정답)
          </span>
        </div>
      {/each}
    </div>
  </div>
</div>
```

**크기**: 100 라인 (적절한 크기)

---

### 6. Game.svelte (Parent)

#### 역할
- Socket.IO 연결 및 이벤트 핸들링
- YouTube Player API 초기화
- 게임 로직 관리
- 자식 컴포넌트에 Props 전달

#### 구조

```typescript
<script lang="ts">
  import { io } from "socket.io-client";
  import { onMount, onDestroy } from "svelte";
  import { gameStore, isHost, updateGameStore } from "./gameStore.js";
  import GameLobby from "./GameLobby.svelte";
  import GameRoom from "./GameRoom.svelte";

  let socket: Socket;
  let $gameStore = $state(get(gameStore));

  onMount(() => {
    // Socket.IO 연결
    socket = io("http://localhost:3000");
    updateGameStore({ socket });

    // 이벤트 핸들러 등록
    socket.on("connect", handleConnect);
    socket.on("disconnect", handleDisconnect);
    socket.on("room-created", handleRoomCreated);
    // ... 20+ 이벤트 핸들러

    // YouTube Player API 초기화
    loadYouTubeAPI();
  });

  onDestroy(() => {
    socket?.disconnect();
  });

  // 이벤트 핸들러들 (20+ 함수)
  function handleConnect() { /* ... */ }
  function handleRoomCreated(data) { /* ... */ }
  function handleGameStarted(data) { /* ... */ }
  // ...
</script>

{#if !$gameStore.currentRoom}
  <GameLobby
    connected={$gameStore.connected}
    nickname={$gameStore.nickname}
    roomCode={$gameStore.roomCode}
    selectedPlaylistId={$gameStore.selectedPlaylistId}
    playlists={$gameStore.playlists}
    onCreateRoom={createRoom}
    onJoinRoom={joinRoom}
    onNicknameChange={(e) => updateGameStore({ nickname: e.target.value })}
    onRoomCodeChange={(e) => updateGameStore({ roomCode: e.target.value })}
    onPlaylistChange={(e) => updateGameStore({ selectedPlaylistId: e.target.value })}
  />
{:else}
  <GameRoom
    currentRoom={$gameStore.currentRoom}
    isHost={$isHost}
    gameStarted={$gameStore.gameStarted}
    currentRound={$gameStore.currentRound}
    totalRounds={$gameStore.totalRounds}
    userAnswer={$gameStore.userAnswer}
    hasSubmitted={$gameStore.hasSubmitted}
    answerResult={$gameStore.answerResult}
    onStartGame={startGame}
    onSubmitAnswer={submitAnswer}
    onAnswerChange={(e) => updateGameStore({ userAnswer: e.target.value })}
    onLeaveRoom={leaveRoom}
  />
{/if}
```

**크기**: 715 라인 (로직 중심, UI는 자식에게 위임)

---

## Playlist 페이지 리팩터링

### 1. PlaylistForm.svelte

#### 역할
- 플레이리스트 생성 모달
- 플레이리스트 수정 모달

#### Props 인터페이스

```typescript
interface Props {
  show: boolean;
  mode: "create" | "edit";
  name: string;
  description: string;
  onClose: () => void;
  onSave: () => void;
  onNameChange: (value: string) => void;
  onDescriptionChange: (value: string) => void;
}
```

#### UI 구조

```svelte
{#if show}
  <div class="modal-backdrop" onclick={onClose}>
    <div class="modal" onclick={(e) => e.stopPropagation()}>
      <h3>
        {mode === "create" ? "새 플레이리스트 만들기" : "플레이리스트 수정"}
      </h3>

      <div class="form-group">
        <label>플레이리스트 이름</label>
        <input
          type="text"
          value={name}
          oninput={(e) => onNameChange(e.currentTarget.value)}
          placeholder="플레이리스트 이름"
        />
      </div>

      <div class="form-group">
        <label>설명</label>
        <textarea
          value={description}
          oninput={(e) => onDescriptionChange(e.currentTarget.value)}
          placeholder="플레이리스트 설명"
          rows="4"
        ></textarea>
      </div>

      <div class="button-group">
        <button class="btn-primary" onclick={onSave}>
          {mode === "create" ? "생성" : "저장"}
        </button>
        <button class="btn-secondary" onclick={onClose}>취소</button>
      </div>
    </div>
  </div>
{/if}
```

**크기**: 147 라인 (적절한 크기)

---

### 2. TrackForm.svelte

#### 역할
- 트랙 추가 모달
- YouTube URL 파싱
- 다중 정답 입력

#### Props 인터페이스

```typescript
interface Props {
  show: boolean;
  youtubeUrl: string;
  videoId: string;
  trackInfo: Track | null;
  loadingTrack: boolean;
  answers: string[];
  onClose: () => void;
  onUrlInput: () => void;
  onYoutubeUrlChange: (value: string) => void;
  onAddTrack: () => void;
  onAddAnswer: () => void;
  onRemoveAnswer: (index: number) => void;
  onUpdateAnswer: (index: number, value: string) => void;
}
```

#### UI 구조

```svelte
{#if show}
  <div class="modal-backdrop" onclick={onClose}>
    <div class="modal" onclick={(e) => e.stopPropagation()}>
      <h3>트랙 추가</h3>

      <!-- YouTube URL 입력 -->
      <div class="form-group">
        <label>YouTube 링크 또는 비디오 ID</label>
        <div class="url-input-group">
          <input
            type="text"
            value={youtubeUrl}
            oninput={(e) => onYoutubeUrlChange(e.currentTarget.value)}
            placeholder="YouTube URL 또는 비디오 ID"
            onkeydown={(e) => e.key === "Enter" && onUrlInput()}
          />
          <button class="btn-search" onclick={onUrlInput}>🔍 검색</button>
        </div>
        {#if videoId}
          <div class="video-id-display">비디오 ID: {videoId}</div>
        {/if}
      </div>

      {#if loadingTrack}
        <div class="loading">트랙 정보 로딩 중...</div>
      {/if}

      <!-- 트랙 정보 -->
      {#if trackInfo}
        <div class="track-info">
          <h4>트랙 정보</h4>
          <p><strong>제목:</strong> {trackInfo.name}</p>
          <p><strong>아티스트:</strong> {trackInfo.artist}</p>

          <!-- 다중 정답 입력 -->
          <div class="answers-section">
            <h4>정답 입력</h4>
            <div class="answers-list">
              {#each answers as answer, index}
                <div class="answer-input-row">
                  <input
                    type="text"
                    value={answer}
                    oninput={(e) => onUpdateAnswer(index, e.currentTarget.value)}
                    placeholder="정답 {index + 1}"
                  />
                  <button onclick={() => onRemoveAnswer(index)}>✕</button>
                </div>
              {/each}
            </div>
            <button onclick={onAddAnswer}>➕ 정답 추가</button>
          </div>

          <div class="track-form-actions">
            <button class="btn-primary" onclick={onAddTrack}>
              플레이리스트에 추가
            </button>
            <button class="btn-secondary" onclick={onClose}>취소</button>
          </div>
        </div>
      {/if}
    </div>
  </div>
{/if}
```

**크기**: 327 라인 (적절한 크기)

---

### 3. TrackList.svelte

#### 역할
- 트랙 목록 표시
- 트랙 정답 수정
- 트랙 삭제

#### Props 인터페이스

```typescript
interface Props {
  tracks: Track[];
  playlistTracks: PlaylistTrack[];
  editingTrackId: string | null;
  editAnswers: string[];
  onStartEdit: (videoId: string) => void;
  onCancelEdit: () => void;
  onUpdateTrack: (videoId: string) => void;
  onRemoveTrack: (videoId: string) => void;
  onAddEditAnswer: () => void;
  onRemoveEditAnswer: (index: number) => void;
  onUpdateEditAnswer: (index: number, value: string) => void;
}
```

#### UI 구조

```svelte
<div class="track-list">
  <h3>트랙 목록 ({tracks.length})</h3>

  {#if tracks.length === 0}
    <div class="empty-message">
      아직 트랙이 없습니다. "➕ 트랙 추가" 버튼을 눌러 추가해보세요!
    </div>
  {:else}
    {#each tracks as track}
      {@const answers = getTrackAnswers(track.id)}
      <div class="track-item-container">
        <!-- 트랙 정보 -->
        <div class="track-item">
          <div class="track-info">
            <div class="track-name">{track.name}</div>
            <div class="track-artist">{track.artist}</div>
            <div class="track-answers">
              <strong>정답:</strong>
              {#if answers.length > 0}
                {answers.join(", ")}
              {:else}
                <span class="no-answers">(YouTube 제목)</span>
              {/if}
            </div>
          </div>
          <div class="track-actions">
            <button onclick={() => onStartEdit(track.id)}>✏️ 수정</button>
            <button onclick={() => onRemoveTrack(track.id)}>🗑️ 삭제</button>
          </div>
        </div>

        <!-- 수정 폼 -->
        {#if editingTrackId === track.id}
          <div class="track-edit-form">
            <h4>정답 수정</h4>
            <div class="answers-list">
              {#each editAnswers as answer, index}
                <div class="answer-input-row">
                  <input
                    type="text"
                    value={answer}
                    oninput={(e) => onUpdateEditAnswer(index, e.currentTarget.value)}
                    placeholder="정답 {index + 1}"
                  />
                  <button onclick={() => onRemoveEditAnswer(index)}>✕</button>
                </div>
              {/each}
            </div>
            <button onclick={onAddEditAnswer}>➕ 정답 추가</button>
            <div class="track-edit-actions">
              <button onclick={() => onUpdateTrack(track.id)}>✅ 저장</button>
              <button onclick={onCancelEdit}>❌ 취소</button>
            </div>
          </div>
        {/if}
      </div>
    {/each}
  {/if}
</div>
```

**크기**: 332 라인 (적절한 크기)

---

### 4. Playlist.svelte (Parent)

#### 역할
- 플레이리스트 CRUD 로직
- 트랙 CRUD 로직
- API 호출 관리
- 자식 컴포넌트에 Props 전달

#### 구조

```typescript
<script lang="ts">
  import { onMount } from "svelte";
  import PlaylistForm from "./PlaylistForm.svelte";
  import TrackForm from "./TrackForm.svelte";
  import TrackList from "./TrackList.svelte";

  // 상태 관리
  let playlists = $state<Playlist[]>([]);
  let selectedPlaylist = $state<Playlist | null>(null);
  let tracks = $state<Track[]>([]);

  // 폼 상태
  let showPlaylistForm = $state(false);
  let formMode = $state<"create" | "edit">("create");
  let formName = $state("");
  let formDescription = $state("");

  let showTrackForm = $state(false);
  let youtubeUrl = $state("");
  let videoId = $state("");
  let trackInfo = $state<Track | null>(null);
  let answers = $state<string[]>([""]); let editingTrackId = $state<string | null>(null);
  let editAnswers = $state<string[]>([]);

  onMount(() => {
    loadPlaylists();
  });

  // API 호출 함수들 (20+ 함수)
  async function loadPlaylists() { /* ... */ }
  async function selectPlaylist(playlist: Playlist) { /* ... */ }
  async function savePlaylist() { /* ... */ }
  async function deletePlaylist() { /* ... */ }
  function extractVideoId(url: string) { /* ... */ }
  async function handleUrlInput() { /* ... */ }
  async function addTrack() { /* ... */ }
  async function removeTrack(videoId: string) { /* ... */ }
  async function updateTrackAnswers(videoId: string) { /* ... */ }
  // ... 나머지 함수들
</script>

<div class="playlist-manager">
  <h1>🎵 플레이리스트 관리</h1>

  <div class="content">
    <!-- 사이드바: 플레이리스트 목록 -->
    <div class="sidebar">
      <!-- ... -->
    </div>

    <!-- 메인: 플레이리스트 상세 -->
    <div class="main-content">
      {#if selectedPlaylist}
        <div class="playlist-header">
          <!-- ... -->
        </div>

        <TrackList
          {tracks}
          playlistTracks={selectedPlaylist.tracks}
          {editingTrackId}
          {editAnswers}
          onStartEdit={startEditTrack}
          onCancelEdit={cancelEditTrack}
          onUpdateTrack={updateTrackAnswers}
          onRemoveTrack={removeTrack}
          onAddEditAnswer={addEditAnswer}
          onRemoveEditAnswer={removeEditAnswer}
          onUpdateEditAnswer={updateEditAnswer}
        />
      {/if}
    </div>
  </div>
</div>

<!-- 모달들 -->
<PlaylistForm
  show={showPlaylistForm}
  mode={formMode}
  name={formName}
  description={formDescription}
  onClose={() => (showPlaylistForm = false)}
  onSave={savePlaylist}
  onNameChange={(value) => (formName = value)}
  onDescriptionChange={(value) => (formDescription = value)}
/>

<TrackForm
  show={showTrackForm}
  {youtubeUrl}
  {videoId}
  {trackInfo}
  {loadingTrack}
  {answers}
  onClose={() => { /* cleanup */ }}
  onUrlInput={handleUrlInput}
  onYoutubeUrlChange={(value) => (youtubeUrl = value)}
  onAddTrack={addTrack}
  onAddAnswer={addAnswer}
  onRemoveAnswer={removeAnswer}
  onUpdateAnswer={updateAnswer}
/>
```

**크기**: 760 라인 (38% 감소, 로직 중심)

---

## App.svelte 간소화

### Before (1,220 lines)

```svelte
<script lang="ts">
  // 모든 게임 로직
  // 모든 Socket.IO 핸들러
  // 모든 YouTube Player 로직
  // 플레이리스트 관리 UI
  // 게임 UI
</script>

<!-- 거대한 UI 템플릿 -->
```

### After (86 lines)

```svelte
<script lang="ts">
  import Game from "./lib/pages/game/Game.svelte";
  import Playlist from "./lib/pages/playlist/Playlist.svelte";

  let currentPage = $state<"game" | "playlist">("game");
</script>

<div class="app">
  <nav class="navbar">
    <h1>🎵 ListenUp!</h1>
    <div class="nav-buttons">
      <button
        class:active={currentPage === "game"}
        onclick={() => (currentPage = "game")}
      >
        🎮 게임
      </button>
      <button
        class:active={currentPage === "playlist"}
        onclick={() => (currentPage = "playlist")}
      >
        🎵 플레이리스트 관리
      </button>
    </div>
  </nav>

  <main class="main-content">
    {#if currentPage === "playlist"}
      <Playlist />
    {:else}
      <Game />
    {/if}
  </main>
</div>

<style>
  /* 간단한 레이아웃 스타일만 */
</style>
```

**개선 효과**:
- ✅ 93% 라인 감소 (1,220 → 86)
- ✅ 라우팅 책임만 집중
- ✅ 각 페이지는 독립적으로 관리

---

## 개선 효과

### 1. 코드 가독성

**Before**:
- 1,220 라인 파일에서 특정 기능 찾기 어려움
- 스크롤 필요 (Ctrl+F에 의존)

**After**:
- 파일명으로 기능 식별 즉시 가능
- `GameLobby.svelte` → 로비 UI
- `TrackForm.svelte` → 트랙 추가 폼

### 2. 유지보수성

**Before**:
- 한 기능 수정 시 연관 코드 찾기 어려움
- 코드 변경 시 부작용 위험 높음

**After**:
- 명확한 Props 인터페이스
- 자식 컴포넌트는 부모에 의존하지 않음
- 단위별 수정 가능

### 3. 협업

**Before**:
- 동일 파일 수정 시 충돌 빈번
- 코드 리뷰 어려움 (diff가 너무 큼)

**After**:
- 기능별 파일 분리로 충돌 최소화
- 작은 단위로 리뷰 가능
- 새로운 팀원 온보딩 쉬움

### 4. 테스트 용이성

**Before**:
- 단위 테스트 불가능
- 통합 테스트만 가능

**After**:
- 각 컴포넌트 독립적으로 테스트 가능
- Props 기반이라 Mock 주입 쉬움

---

## 코드 메트릭

### 파일 크기 비교

| 파일 | Before | After | 감소율 |
|------|--------|-------|--------|
| App.svelte | 1,220 lines | 86 lines | 93% ⬇️ |
| PlaylistManager.svelte | 1,229 lines | - | - |
| Playlist.svelte | - | 760 lines | - |
| PlaylistForm.svelte | - | 147 lines | - |
| TrackForm.svelte | - | 327 lines | - |
| TrackList.svelte | - | 332 lines | - |
| **Total (Playlist)** | **1,229 lines** | **1,566 lines** | 27% ⬆️* |

*총 라인 수는 증가했지만, 관리 가능한 크기로 분리됨

### 컴포넌트 개수

| 구분 | Before | After |
|------|--------|-------|
| Game 페이지 | 1 | 6 |
| Playlist 페이지 | 1 | 4 |
| **Total** | **2** | **10** |

### 평균 파일 크기

| 구분 | Before | After |
|------|--------|-------|
| Game 페이지 | 1,220 lines | 291 lines |
| Playlist 페이지 | 1,229 lines | 391 lines |

### 컴포넌트별 책임

| 컴포넌트 | 책임 개수 | 적정성 |
|----------|-----------|--------|
| GameLobby.svelte | 1 (로비 UI) | ✅ 적정 |
| GameRoom.svelte | 2 (게임 UI + 결과) | ✅ 적정 |
| GamePlayer.svelte | 1 (플레이어) | ✅ 적정 |
| GameResult.svelte | 1 (결과) | ✅ 적정 |
| PlaylistForm.svelte | 1 (폼 UI) | ✅ 적정 |
| TrackForm.svelte | 1 (폼 UI) | ✅ 적정 |
| TrackList.svelte | 2 (목록 + 수정) | ✅ 적정 |

---

## 향후 개선 사항

### 단기 개선 (1-2주)

1. **공통 컴포넌트 분리**
   ```
   lib/
   └── components/
       ├── Modal.svelte          ← 공통 모달
       ├── Button.svelte         ← 공통 버튼
       └── Input.svelte          ← 공통 입력
   ```

2. **타입 정의 파일 분리**
   ```
   lib/
   └── types/
       ├── game.ts               ← 게임 타입
       └── playlist.ts           ← 플레이리스트 타입
   ```

3. **유틸리티 함수 분리**
   ```
   lib/
   └── utils/
       ├── youtube.ts            ← YouTube 관련
       └── validation.ts         ← 검증 함수
   ```

### 중기 개선 (1-2개월)

1. **Store 패턴 통일**
   - 모든 페이지에 Store 적용
   - `playlistStore.ts` 추가

2. **라우터 라이브러리 도입**
   ```typescript
   import { Router, Route } from "svelte-routing";

   <Router>
     <Route path="/" component={Game} />
     <Route path="/playlist" component={Playlist} />
     <Route path="/settings" component={Settings} />
   </Router>
   ```

3. **컴포넌트 문서화**
   - Storybook 도입
   - Props 문서 자동 생성

### 장기 개선 (3-6개월)

1. **디자인 시스템 구축**
   - 공통 스타일 가이드
   - 컬러 팔레트
   - 타이포그래피

2. **상태 관리 라이브러리**
   - Zustand 또는 Pinia 도입
   - 복잡한 상태 관리 개선

3. **마이크로 프론트엔드**
   - 게임 / 플레이리스트 완전 분리
   - 독립 배포 가능한 구조

---

## 결론

### 달성한 목표

1. ✅ **파일 크기 감소**: App.svelte 93% 감소
2. ✅ **기능별 분리**: 10개 컴포넌트로 명확히 구분
3. ✅ **가독성 향상**: 파일명으로 기능 즉시 파악
4. ✅ **유지보수성**: 독립적인 컴포넌트 수정 가능
5. ✅ **빌드 성공**: 타입 오류 없이 전체 빌드 성공
6. ✅ **기능 유지**: 모든 기능 손실 없이 동작

### 기술적 성과

- Svelte 5 Runes API 적극 활용
- TypeScript 인터페이스로 Props 타입 안정성 확보
- Props 패턴으로 명확한 데이터 흐름
- 단일 책임 원칙 적용으로 응집도 향상

### 팀 협업 개선

- 기능별 파일 분리로 충돌 최소화
- 코드 리뷰가 쉬워짐 (작은 단위)
- 새로운 팀원 온보딩 시간 단축
- 기능 추가 시 명확한 위치 파악

### 학습 포인트

1. **컴포넌트 분리 기준**
   - 200~400 라인이 적정
   - 독립적인 UI 섹션 단위
   - 재사용 가능성 고려

2. **Props 패턴**
   - 부모가 상태 소유
   - 자식은 Props로 수신
   - 이벤트 핸들러도 Props로 전달

3. **폴더 구조**
   - Pages 기반 구조 (확장성)
   - 기능별 응집도 극대화
   - 명확한 네이밍 규칙

---

## 참고 자료

### 코드 위치

**Game 페이지**:
- `packages/client/src/lib/pages/game/Game.svelte`
- `packages/client/src/lib/pages/game/GameLobby.svelte`
- `packages/client/src/lib/pages/game/GameRoom.svelte`
- `packages/client/src/lib/pages/game/GamePlayer.svelte`
- `packages/client/src/lib/pages/game/GameResult.svelte`
- `packages/client/src/lib/pages/game/gameStore.ts`

**Playlist 페이지**:
- `packages/client/src/lib/pages/playlist/Playlist.svelte`
- `packages/client/src/lib/pages/playlist/PlaylistForm.svelte`
- `packages/client/src/lib/pages/playlist/TrackForm.svelte`
- `packages/client/src/lib/pages/playlist/TrackList.svelte`

**기타**:
- `packages/client/src/App.svelte`
- `packages/client/src/lib/pages/playlist/Playlist.svelte.backup` (원본 백업)

### 관련 문서

- [플레이리스트 관리 시스템 구현 보고서](./플레이리스트%20관리%20시스템%20구현%20보고서.md)
- [플레이리스트 다중 정답 기능 업데이트 보고서](./플레이리스트%20다중%20정답%20기능%20업데이트%20보고서.md)
- [게임 시스템 구현 보고서](./게임%20시스템%20구현%20보고서.md)

### 외부 참조

- [Svelte 5 Documentation](https://svelte-5-preview.vercel.app/)
- [Component Composition Patterns](https://svelte.dev/docs/composition)
- [TypeScript Props Pattern](https://www.typescriptlang.org/docs/handbook/utility-types.html)

---

**작성일**: 2025-11-13
**작성자**: ListenUp! Team
**버전**: 1.0.0
