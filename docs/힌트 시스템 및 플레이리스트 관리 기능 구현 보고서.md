# 힌트 시스템 및 플레이리스트 관리 기능 구현 보고서

**작성일**: 2025-11-29
**프로젝트**: ListenUp! - 실시간 음악 맞추기 게임
**작업 범위**: 게임 힌트 시스템 완성 + 플레이리스트 관리 페이지 힌트 편집/추가 기능

---

## 📋 목차

1. [작업 개요](#작업-개요)
2. [구현 내용](#구현-내용)
3. [기술적 상세](#기술적-상세)
4. [파일 변경 사항](#파일-변경-사항)
5. [사용 방법](#사용-방법)
6. [테스트 결과](#테스트-결과)
7. [향후 개선 사항](#향후-개선-사항)
8. [결론](#결론)

---

## 작업 개요

### 프로젝트 정보
- **작업 일자**: 2025-11-29
- **작업자**: ListenUp! Team
- **목적**: 게임 진행 중 힌트 자동 표시 + 플레이리스트 관리 페이지에서 힌트 관리 기능 제공

### 구현 배경

게임의 난이도 조절과 사용자 경험 개선을 위해 힌트 시스템을 완성하고, 관리자가 플레이리스트를 관리할 때 힌트를 쉽게 추가/수정할 수 있는 UI를 구현했습니다.

#### 주요 기능
1. **게임 중 힌트 자동 표시**:
   - 각 트랙의 설정된 시간에 힌트가 자동으로 화면에 표시
   - 그라데이션 배경의 시각적으로 눈에 띄는 힌트 카드
   - 5초 후 자동 사라짐 또는 수동 닫기 가능
   - 힌트 순서 표시 (예: "힌트 1/2")

2. **플레이리스트 관리 - 트랙 수정**:
   - 기존 트랙의 정답과 힌트를 함께 수정
   - 정답 섹션과 힌트 섹션을 border로 시각적 구분
   - 각 힌트마다 시간(초)과 텍스트 개별 설정

3. **플레이리스트 관리 - 트랙 추가**:
   - 새 트랙 추가 시 정답과 힌트를 함께 입력
   - 왼쪽에 딜레이 시간, 오른쪽에 힌트 텍스트 입력
   - 정답/힌트 섹션 색상으로 구분 (초록/보라)

---

## 구현 내용

### 1. 힌트 시스템 완성 (게임 중)

이전 세션에서 백엔드 힌트 타이머 로직을 구현했으며, 이번에는 전체 시스템을 완성했습니다.

#### 1.1 데이터 구조
```json
{
  "videoId": "dQw4w9WgXcQ",
  "answers": ["Never Gonna Give You Up", "Rick Astley"],
  "hints": [
    { "showAtSeconds": 8, "text": "💡 1987년에 발매된 곡입니다" },
    { "showAtSeconds": 15, "text": "💡 인터넷 밈으로 유명한 곡이에요" }
  ]
}
```

#### 1.2 서버 힌트 타이머
- 라운드 시작 시 각 힌트의 `showAtSeconds`에 맞춰 타이머 설정
- 지정된 시간에 `hint-shown` 소켓 이벤트 발송
- 라운드 종료 시 모든 힌트 타이머 정리 (메모리 누수 방지)

#### 1.3 클라이언트 힌트 표시
- Hint.svelte 컴포넌트로 시각적 힌트 카드 렌더링
- 그라데이션 배경 (보라색 계열)
- fade/scale 애니메이션
- 5초 후 자동 사라짐
- 닫기 버튼으로 수동 닫기

### 2. 플레이리스트 관리 - 트랙 수정 기능

기존 트랙의 정답뿐만 아니라 힌트도 수정할 수 있도록 UI 확장

#### 2.1 서버 API 수정
```typescript
// playlistService.updateTrack
async updateTrack(
  playlistId: string,
  videoId: string,
  answers: string[],
  hints?: { showAtSeconds: number; text: string }[]
)
```

```typescript
// PUT /api/playlists/:id/tracks/:videoId
const { answers, hints } = req.body;
await playlistService.updateTrack(id, videoId, answers, hints);
```

#### 2.2 클라이언트 UI 구조
```svelte
<div class="track-edit-form">
  <!-- 정답 섹션 -->
  <div class="edit-section">
    <h4>정답 수정</h4>
    <!-- 정답 입력 필드들 -->
    <button class="btn-add-answer">➕ 정답 추가</button>
  </div>

  <!-- 힌트 섹션 -->
  <div class="edit-section">
    <h4>힌트 수정</h4>
    <!-- 힌트 입력 필드들 -->
    <button class="btn-add-hint">➕ 힌트 추가</button>
  </div>
</div>
```

#### 2.3 UI 특징
- **정답 섹션**: 흰색 배경, 회색 테두리
- **힌트 섹션**: 흰색 배경, 회색 테두리
- 각 섹션이 border로 명확히 구분
- 힌트 입력: 왼쪽 100px 시간 input + 오른쪽 flex-1 텍스트 input
- "➕ 힌트 추가" 버튼은 보라색 (#9c27b0)

### 3. 플레이리스트 관리 - 트랙 추가 기능

새 트랙 추가 시 힌트도 함께 입력할 수 있도록 확장

#### 3.1 서버 API 수정
```typescript
// playlistService.addTrack
async addTrack(
  playlistId: string,
  videoId: string,
  answers: string[] = [],
  hints?: { showAtSeconds: number; text: string }[]
)
```

```typescript
// POST /api/playlists/:id/tracks
const { videoId, answers, hints } = req.body;
await playlistService.addTrack(id, videoId, answers, hints);
```

#### 3.2 TrackForm UI 구조
```svelte
<!-- 정답 섹션 -->
<div class="answers-section">
  <h4>정답 입력</h4>
  <p class="hint-text">게임에서 인정할 정답을 입력하세요...</p>
  <!-- 정답 입력 -->
</div>

<!-- 힌트 섹션 -->
<div class="hints-section">
  <h4>힌트 입력</h4>
  <p class="hint-text">게임 중 나타날 힌트를 입력하세요...</p>
  <!-- 힌트 입력 -->
</div>
```

#### 3.3 시각적 구분
- **정답 섹션**: 회색 배경 + 초록색 상단 테두리 (`border-top: 3px solid #4caf50`)
- **힌트 섹션**: 회색 배경 + 보라색 상단 테두리 (`border-top: 3px solid #9c27b0`)
- 정답 추가 버튼: 초록색
- 힌트 추가 버튼: 보라색

---

## 기술적 상세

### 1. 타입 정의

#### 서버 (TypeScript)
```typescript
// types/index.ts
export interface Hint {
  showAtSeconds: number;
  text: string;
}

export interface PlaylistTrack {
  videoId: string;
  answers: string[];
  hints?: Hint[];
}

export interface GameState {
  // ... 기존 필드
  hintTimers: NodeJS.Timeout[];
}
```

#### 클라이언트 (Svelte/TypeScript)
```typescript
// Playlist.svelte, TrackList.svelte, TrackForm.svelte
interface Hint {
  showAtSeconds: number;
  text: string;
}

interface PlaylistTrack {
  videoId: string;
  answers: string[];
  hints?: Hint[];
}

// gameStore.ts
interface GameState {
  // ... 기존 필드
  currentHint: { text: string; index: number; total: number } | null;
}
```

### 2. 힌트 타이머 관리

```typescript
// game.handler.ts
function setupHintTimers(io: Server, roomCode: string, room: Room, track: Track): void {
  clearHintTimers(room);

  if (!track.hints || track.hints.length === 0) return;

  track.hints.forEach((hint, index) => {
    const timer = setTimeout(() => {
      io.to(roomCode).emit(events.HINT_SHOWN, {
        roundNumber: room.gameState.currentRound,
        hint: {
          text: hint.text,
          index: index + 1,
          total: track.hints!.length
        }
      });
    }, hint.showAtSeconds * 1000);

    room.gameState.hintTimers.push(timer);
  });
}

function clearHintTimers(room: Room): void {
  room.gameState.hintTimers.forEach(timer => clearTimeout(timer));
  room.gameState.hintTimers = [];
}
```

### 3. 클라이언트 힌트 표시

```typescript
// Game.svelte
socket.on("hint-shown", (data) => {
  updateGameStore({ currentHint: data.hint });

  // 5초 후 자동 숨김
  setTimeout(() => {
    updateGameStore({ currentHint: null });
  }, 5000);
});
```

```svelte
<!-- Game.svelte -->
{#if currentHint}
  <Hint
    text={currentHint.text}
    index={currentHint.index}
    total={currentHint.total}
    onDismiss={() => updateGameStore({ currentHint: null })}
  />
{/if}
```

### 4. 플레이리스트 관리 핸들러

```typescript
// Playlist.svelte
// 트랙 수정용 힌트 핸들러
function addEditHint() {
  editHints = [...editHints, { showAtSeconds: 8, text: "" }];
}

function removeEditHint(index: number) {
  editHints = editHints.filter((_, i) => i !== index);
}

function updateEditHintTime(index: number, value: number) {
  editHints[index] = { ...editHints[index], showAtSeconds: value };
}

function updateEditHintText(index: number, value: string) {
  editHints[index] = { ...editHints[index], text: value };
}

// 트랙 추가용 힌트 핸들러
function addHint() {
  hints = [...hints, { showAtSeconds: 8, text: "" }];
}

function removeHint(index: number) {
  hints = hints.filter((_, i) => i !== index);
}

function updateHintTime(index: number, value: number) {
  hints[index] = { ...hints[index], showAtSeconds: value };
}

function updateHintText(index: number, value: string) {
  hints[index] = { ...hints[index], text: value };
}
```

---

## 파일 변경 사항

### 서버 (4개 파일 수정)

1. **`/packages/server/src/types/index.ts`**
   - `Hint` 인터페이스 추가 (이미 이전에 완료)
   - `PlaylistTrack`에 `hints?: Hint[]` 추가 (이미 이전에 완료)
   - `GameState`에 `hintTimers: NodeJS.Timeout[]` 추가 (이미 이전에 완료)

2. **`/packages/server/src/services/playlist.ts`**
   - `updateTrack()`: hints 파라미터 추가
   - `addTrack()`: hints 파라미터 추가

3. **`/packages/server/src/index.ts`**
   - `PUT /api/playlists/:id/tracks/:videoId`: hints 처리 추가
   - `POST /api/playlists/:id/tracks`: hints 처리 추가

4. **`/packages/server/src/data/playlists.json`**
   - 모든 플레이리스트의 모든 트랙에 hints 추가 (20개 트랙)

### 클라이언트 (3개 파일 수정)

5. **`/packages/client/src/lib/pages/playlist/Playlist.svelte`**
   - `Hint` 인터페이스 추가
   - `PlaylistTrack`에 `hints?: Hint[]` 추가
   - 트랙 추가용 상태: `hints` 추가
   - 트랙 수정용 상태: `editHints` 추가
   - 힌트 관련 핸들러 8개 추가
   - `updateTrack()`: hints 전송 추가
   - `addTrack()`: hints 전송 추가
   - TrackList에 힌트 props 전달
   - TrackForm에 힌트 props 전달

6. **`/packages/client/src/lib/pages/playlist/TrackList.svelte`**
   - `Hint` 인터페이스 추가
   - `PlaylistTrack`에 `hints?: Hint[]` 추가
   - Props에 `editHints` 및 힌트 핸들러 4개 추가
   - `getTrackHints()` 헬퍼 함수 추가
   - 트랙 정보 표시에 힌트 표시 추가
   - 수정 폼에 정답/힌트 섹션 구분 추가
   - 힌트 입력 UI 추가
   - 힌트 관련 CSS 스타일 추가

7. **`/packages/client/src/lib/pages/playlist/TrackForm.svelte`**
   - `Hint` 인터페이스 추가
   - Props에 `hints` 및 힌트 핸들러 4개 추가
   - 정답 섹션과 힌트 섹션 구분 (색상 테두리)
   - 힌트 입력 UI 추가
   - 힌트 관련 CSS 스타일 추가

### 통계
- **총 수정 파일**: 7개
- **서버**: 4개 파일
- **클라이언트**: 3개 파일
- **새로운 타입**: `Hint` 인터페이스
- **새로운 API 파라미터**: hints (POST, PUT)
- **새로운 컴포넌트**: 없음 (기존 컴포넌트 확장)

---

## 사용 방법

### 1. 플레이리스트 관리에서 힌트 추가

#### 새 트랙 추가 시
1. 플레이리스트 관리 페이지 접속
2. 플레이리스트 선택
3. "➕ 트랙 추가" 클릭
4. YouTube URL 입력 후 검색
5. **정답 입력** 섹션 (초록색 테두리):
   - 정답 입력
   - "➕ 정답 추가"로 정답 추가
6. **힌트 입력** 섹션 (보라색 테두리):
   - 왼쪽: 시간(초) 입력 (예: 8)
   - 오른쪽: 힌트 텍스트 입력 (예: "💡 1987년에 발매된 곡입니다")
   - "➕ 힌트 추가"로 힌트 추가
7. "플레이리스트에 추가" 클릭

#### 기존 트랙 수정 시
1. 트랙 목록에서 "✏️ 수정" 클릭
2. **정답 수정** 섹션 (흰색 배경, 회색 테두리):
   - 정답 수정/추가/삭제
3. **힌트 수정** 섹션 (흰색 배경, 회색 테두리):
   - 왼쪽: 시간(초) 수정
   - 오른쪽: 힌트 텍스트 수정
   - "➕ 힌트 추가"로 새 힌트 추가
   - "✕"로 힌트 삭제
4. "✅ 저장" 클릭

### 2. 게임 중 힌트 확인

1. 방 생성 및 게임 시작
2. 라운드 진행 중 설정된 시간에 힌트 자동 표시
3. 힌트 카드 표시:
   - 중앙 오버레이로 표시
   - "힌트 1/2" 형식으로 순서 표시
   - 힌트 내용 표시
4. 5초 후 자동 사라짐 또는 닫기 버튼 클릭

---

## 테스트 결과

### 빌드 테스트
```bash
# 서버 빌드
cd /workspaces/ListenUp/packages/server
npm run build
# ✅ 성공

# 클라이언트 빌드
cd /workspaces/ListenUp/packages/client
npm run build
# ✅ 성공 (접근성 경고는 기존 이슈)
```

### 기능 테스트 시나리오

#### 1. 트랙 추가 테스트
- ✅ 정답만 입력하고 트랙 추가 → 힌트 없이 저장됨
- ✅ 정답 + 힌트 입력하고 트랙 추가 → 정상 저장됨
- ✅ 여러 개 힌트 추가 → 모두 저장됨
- ✅ 힌트 삭제 후 추가 → 정상 동작

#### 2. 트랙 수정 테스트
- ✅ 기존 트랙에 힌트 추가 → 정상 저장됨
- ✅ 기존 힌트 수정 → 정상 저장됨
- ✅ 기존 힌트 삭제 → 정상 저장됨
- ✅ 정답과 힌트 동시 수정 → 정상 저장됨

#### 3. 게임 중 힌트 표시 테스트 (이전 세션)
- ✅ 8초에 첫 번째 힌트 표시
- ✅ 15초에 두 번째 힌트 표시
- ✅ 5초 후 자동 사라짐
- ✅ 닫기 버튼으로 수동 닫기
- ✅ 라운드 종료 시 타이머 정리

#### 4. UI/UX 테스트
- ✅ 정답/힌트 섹션이 색상으로 명확히 구분됨
- ✅ 시간 입력 필드 100px, 텍스트 필드 flex-1로 적절한 크기
- ✅ 버튼 색상이 섹션과 일치 (정답=초록, 힌트=보라)
- ✅ focus 시 적절한 색상으로 테두리 강조

---

## 향후 개선 사항

### 1. 힌트 미리보기
- 플레이리스트 관리에서 입력한 힌트를 게임처럼 미리보기
- 힌트 표시 타이밍 시뮬레이션

### 2. 힌트 템플릿
- 자주 사용하는 힌트 패턴 저장
- 연도, 장르, 아티스트 등의 템플릿 제공

### 3. 힌트 난이도 조절
- 쉬운 힌트 / 어려운 힌트 구분
- 게임 설정에서 힌트 난이도 선택

### 4. 힌트 통계
- 가장 도움이 된 힌트 분석
- 힌트 표시 후 정답률 추적

### 5. 접근성 개선
- 모달/오버레이에 키보드 이벤트 핸들러 추가
- ARIA role 속성 추가
- label과 input 연결

---

## 결론

### 주요 성과

1. **완전한 힌트 시스템 구축**:
   - 서버: 타이머 관리, 소켓 이벤트 발송
   - 클라이언트: 시각적 힌트 카드 표시
   - 데이터: 20개 트랙에 힌트 추가

2. **직관적인 플레이리스트 관리 UI**:
   - 트랙 추가 시 힌트 입력 가능
   - 트랙 수정 시 힌트 편집 가능
   - 정답/힌트 섹션을 색상으로 명확히 구분

3. **확장 가능한 구조**:
   - 타입 정의가 명확하여 확장 용이
   - 서버 API가 유연하게 설계됨
   - 컴포넌트 재사용성 고려

### 기술적 의의

- **타입 안정성**: TypeScript를 활용한 완전한 타입 정의
- **실시간 동기화**: Socket.IO를 통한 힌트 실시간 전달
- **메모리 관리**: 타이머 정리를 통한 메모리 누수 방지
- **UI/UX**: 색상 코딩을 통한 직관적인 인터페이스

### 프로젝트 영향

이번 구현으로 ListenUp! 게임의 난이도 조절이 가능해졌으며, 관리자가 쉽게 힌트를 관리할 수 있게 되었습니다. 이는 게임의 접근성을 높이고 사용자 경험을 개선하는 중요한 기능입니다.

---

**문서 버전**: 1.0
**최종 수정일**: 2025-11-29
